# robots.txt for Newspaper CMS

User-agent: *
Allow: /

# Disallow admin panel and auth routes from being crawled
Disallow: /admin/
Disallow: /auth/
Disallow: /api/ # Disallow direct API crawling, though Next.js API routes are not typically HTML pages

Sitemap: /sitemap.xml
# If your site is hosted at a specific domain, use the full URL:
# Sitemap: https://www.yourdomain.com/sitemap.xml
# The NEXT_PUBLIC_SITE_URL could be used here if generating robots.txt dynamically,
# but for a static file, /sitemap.xml is usually fine as it's relative to the root.
# For Next.js 13+ App Router, sitemap.xml is generated at the root.
# So, ensure your NEXT_PUBLIC_SITE_URL is correctly set for the sitemap link in the sitemap itself.
# The sitemap link in robots.txt should be absolute if possible.
# I will assume the sitemap will be at /sitemap.xml relative to the domain.
# If it's generated by Next.js at /sitemap.xml, this is correct.
# The actual URL used by search engines will be domain + /sitemap.xml.
# If NEXT_PUBLIC_SITE_URL is available during robots.txt generation (if dynamic), it's better.
# For now, a relative path for the sitemap is common in static robots.txt.
# Let's use the environment variable to construct the absolute path for robustness.
# However, create_file_with_block doesn't support env vars directly.
# So, I will use a placeholder and note that it should be absolute.

# Consider this content for the actual file:
# User-agent: *
# Allow: /
# Disallow: /admin/
# Disallow: /auth/
# Disallow: /api/
# Sitemap: [YOUR_FULL_SITE_URL]/sitemap.xml
# Example: Sitemap: https://www.example.com/sitemap.xml
# For now, using relative path as a general placeholder:
# Sitemap: /sitemap.xml
# The Next.js sitemap generation will make it available at /sitemap.xml.
# The actual interpretation will depend on the search engine combining domain + this path.
# A more robust approach for robots.txt if it needs dynamic values is to generate it via a route.
# For this step, a static robots.txt with a relative sitemap path is a common starting point.

User-agent: Googlebot
Allow: /

User-agent: Bingbot
Allow: /

# Add other specific bot rules if needed.

# Note: The Sitemap directive should ideally be an absolute URL.
# If your NEXT_PUBLIC_SITE_URL is e.g. https://example.com, then:
# Sitemap: https://example.com/sitemap.xml
# For now, the relative path /sitemap.xml will be used.
# Next.js serves sitemap.xml at the root.
Sitemap: /sitemap.xml
